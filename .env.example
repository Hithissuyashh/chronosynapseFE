# Public URL of the FastAPI science backend.
# Local development (default when unset): http://127.0.0.1:8000
# On Vercel, set this to the HTTPS URL of your deployed backend, otherwise the
# browser cannot reach it and the UI will show a "backend unreachable" state.
VITE_SCIENCE_API_URL=https://your-backend.example.com
